[project]
name = "proxyme"
version = "0.1.0"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Dayron Salazar", email = "dsalazarcazanhas@gmail.com" },
]
dependencies = [
    "PySide6>=6.6",
    "paramiko>=4.0",
    "bcrypt>=4.0",
    "cryptography>=41.0",
    "PyNaCl>=1.5",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.0",
    "ruff>=0.9",
    "pytest>=8.0",
    "pytest-mock>=3.14",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["proxyme"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
addopts = "-ra"
